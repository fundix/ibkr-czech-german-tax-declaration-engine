# src/mcp_server/ — local MCP server exposing the tax engine to Claude
# (Claude Code / Claude Desktop) via stdio. Thin tool wrappers over
# src/webapp/services.RunService — the same layer the web GUI uses.
